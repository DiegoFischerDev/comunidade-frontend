'use client';

import { SupportTicketBar } from './SupportTicketBar';

/**
 * Barra de suporte no layout do dashboard (navega para "Reclame aqui").
 */
export function SupportTicketRoot() {
  return <SupportTicketBar />;
}
